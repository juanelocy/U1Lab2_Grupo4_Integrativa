import { TestimonialCarousel } from './TestimonialCarousel.js';

document.addEventListener('DOMContentLoaded', () => {
  const app = document.querySelector('#app');
  new TestimonialCarousel(app);
});
